# NOTE: not wired into CI right now (the Release workflow is paused). cmd/thaimaturgy
# is the Fyne desktop GUI, which needs CGO + OpenGL/X11 and can't be cross-built with
# CGO_ENABLED=0 as configured below. A real multiplatform GUI release needs fyne-cross
# (or per-OS runners); this config is kept as a starting point for that work.
version: 2

project_name: thaimaturgy

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: thaimaturgy
    main: ./cmd/thaimaturgy
    binary: thaimaturgy
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.Version={{.Version}}
      - -X main.Commit={{.Commit}}
      - -X main.BuildTime={{.Date}}

archives:
  - id: default
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch

release:
  github:
    owner: theburrowhub
    name: thaimaturgy
  draft: false
  prerelease: auto
  name_template: "v{{.Version}}"
