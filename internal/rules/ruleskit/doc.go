// Package ruleskit contains the deliberately small implementation toolkit used
// by Thaimaturgy's built-in reference rulesets. It is not another rules engine:
// packages still own their inputs, continuations, random validation, and result
// semantics. The toolkit only centralizes the stateless protocol boilerplate.
package ruleskit
