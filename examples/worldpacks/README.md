# Example worldpack catalogs

Generated by:

```bash
make worldpack-examples
```

These JSON files are **not loaded** by thAImaturgy at runtime. Each is a
`worldpack/v1` catalog describing a full queryable world: regions, cities,
locations, NPCs, bestiary, items, encounter tables, and world-query tool bindings.

| File | Lines | Setting |
|------|------:|---------|
| `dnd5e_shattered_vale.json` | ~4300 | The Shattered Vale — generic D&D 5e fantasy |

Inspect:

```bash
bin/worldpack-gen -template dnd5e -inspect
```
